{
  "name": "foo",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build-native": "pnpx -p @napi-rs/cli@1.2.1 napi build --platform --release --cargo-cwd . native"
  },
  "devDependencies": {
    "@napi-rs/cli": "^1.3.3"
  }
}
